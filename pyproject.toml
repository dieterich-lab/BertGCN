[tool.poetry]
name = "bertgcn"
version = "0.1.0"
description = "BERT-GCN research prototype"
authors = ["Your Name <your.email@example.com>"]

[tool.poetry.dependencies]
python = "^3.8"
torch = "^2.0.0"
transformers = "^4.21.0"
scikit-learn = "^1.1.0"
scipy = "^1.9.0"
numpy = "^1.24.0"
datasets = "^2.0.0"
pandas = "^1.5.0"
argparse = "^1.4.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
