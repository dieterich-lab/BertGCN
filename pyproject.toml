[project]
name = "bertgcn_clinic"
version = "0.1.0"
description = "A project to classify doctoral letters using BERT and GCN."
authors = [{ name = "Your Name", email = "your@email.com" }]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"